import PageShell from "./_components/page-shell";

/* The canonical home page. Everything lives in <PageShell>, which /homev2 also
   renders — see that file for why there is no fork. */
export default function Home() {
  return <PageShell />;
}
